spring:
  jpa:
    hibernate:
    show-sql: true
    ddl-auto: validate
    #database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    #open-in-view: false
    #generate-ddl: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/project_books
    username: Admin
    password: admin
  #liquibase:
   # change-log: classpath:liquibase/changelog.yml
